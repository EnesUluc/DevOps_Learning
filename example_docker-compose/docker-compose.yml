version: '3'

services:
  web:
    build: ./web
    ports: 
      - "80:80"
    volumes:
      - ./web:/usr/share/nginx/html
    depends_on:
      - db
  db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: example
